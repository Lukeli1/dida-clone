# 2026-06-27 Work Log

## 任务右键菜单增强方案
- 以滴答清单官方任务列表右键菜单截图（含日期/优先级/置顶/标签/创建副本/删除等）为基准
- 分析当前 TaskItem.tsx 右键菜单：仅 3 项（重命名、归档/恢复、删除）
- 完成独立改进文档：`C:\Users\50441\.workbuddy\plans\stellar-beacon-tesla.md`
- 方案覆盖：DateSection、PrioritySection、ActionMenu、TagPopover、MoveToListPopover
- 工期：P0 约 5 天，全部约 9 天

## 右键菜单功能改进文档
- 基于截图分析，聚焦用户指定的6项功能：日期快捷、优先级快捷、置顶、标签子菜单、创建副本、删除
- 生成详细改进文档：`improvement-analysis/右键菜单功能改进文档.md`
- 方案包含：逐项现状分析、代码改动点汇总、5阶段实施路线图、最终右键菜单形态图
- 关键发现：Task.ts/commands.rs/db.rs 均缺少 `pinned` 字段，需添加列兼容逻辑

## 项目版本确认
- 当前版本号：1.6.0（package.json + tauri.conf.json 一致）

## 项目代码审查
- 分析了整体架构（React + TS + Zustand + Tauri v2）
- 主要发现：App.tsx 62k 膨胀问题优先解决；筛选/排序逻辑可抽独立 hook；自动归档不应全量 reload；通知提醒 ID 需持久化；uiStore 领域混杂可拆

## 字体切换功能改进文档
- 完成文档：`improvement-analysis/字体切换功能改进文档.md`
- 方案：CSS 变量 + localStorage 持久化，与现有主题机制一致
- 预设 7 种字体（系统默认/苹方/微软雅黑/思源黑体/霞鹜文楷/思源宋体/JetBrains Mono）+ 自定义输入
- 涉及文件：新增 src/utils/font.ts，修改 index.css / tailwind.config.js / SettingsView.tsx / App.tsx
- 实现步骤：6 步，从配置到构建

## 架构审查与改进方案
- 全面审查代码库（1538行App.tsx、18个组件、5个store、api层等）
- 完成文档：`improvement-analysis/架构改进方案.md`
- 9个改进方向，按P0/P1/P2优先级排列
- P0核心发现：App.tsx过度膨胀、TaskItem 29个prop深度drilling、筛选逻辑分散在两处
- 提出：拆4个hook、Context收敛props、React Query接入、uiStore拆分、深色模式迁移!important等
- 给出建议执行顺序：①拆hook → ②统一筛选 → ③Context收敛 → ④拆store → ...
