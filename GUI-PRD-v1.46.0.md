# GUI 优化 PRD：自适应三栏、紧凑任务列表与日历交互

版本：v1.46.0 GUI 第一阶段  
日期：2026-09-03  
状态：待开发  
适用端：Tauri 桌面端（React + TypeScript）

## 1. 背景

当前版本正在完善日历默认视图偏好与日/周视图自动定位当前时间。本阶段 GUI 优化不改变任务数据语义、后端命令、数据库结构或同步协议，重点改善高频任务管理场景中的信息密度、窗口适配和日历操作可靠性。

## 2. 产品目标

1. 宽屏下保留“左侧导航 + 中央内容 + 右侧详情”的高效率工作流。
2. 窄窗口下不出现标题、日期、标签和操作按钮互相遮挡。
3. 普通桌面窗口一次可扫描约 18～25 条普通任务。
4. 日/周视图首次进入时能看到当前本地时间附近，且不抢占用户手动滚动。
5. 拖拽、编辑和保存失败时给出清晰反馈，降低误操作成本。

## 3. 范围

### 3.1 自适应三栏布局

- 宽屏：显示左侧导航、中央任务/日历、右侧详情。
- 中等宽度：左侧导航可折叠，中央区域保持主要宽度。
- 窄屏：详情面板改为抽屉或覆盖层，不挤压中央内容。
- 左侧导航折叠后保留可识别图标和 tooltip。
- 窗口缩放过程中不得出现明显横向溢出或控件重叠。

建议断点（可根据现有布局微调）：

- ≥1100px：三栏布局。
- 900～1099px：左侧导航可折叠。
- <900px：详情面板使用抽屉。
- <700px：日历工具栏允许换行或收纳到更多菜单。

### 3.2 任务列表密度和稳定性

- 普通任务行高度控制在 36～44px 范围。
- 标题最多显示两行，超出使用省略号。
- 日期、优先级、标签、重复标记放在稳定区域。
- hover 操作按钮使用预留空间，不能导致行宽或行高跳动。
- 覆盖长标题、多个标签、逾期任务、已完成任务和窄屏场景。
- 若已有多选/快捷操作，只做布局兼容，不重做业务逻辑。

### 3.3 日历交互

- 日/周/月视图使用一致的顶部工具栏和导航语义。
- 清晰区分当前日期、当前时间线和用户选中日期。
- 日/周视图挂载时定位到当前本地时间附近，仅执行一次。
- 用户手动滚动后不得被自动定位再次抢回。
- 全天任务与具体时间任务明显分区。
- 月视图任务过多时显示“还有 N 项”，不得撑坏日历格子。
- 拖拽任务时显示目标日期/时间预览。
- 拖拽重复任务时明确修改范围（仅本次/后续/全部，按现有能力实现）。
- 复用现有撤销机制为改期提供撤销；若暂无机制，不扩展全局撤销系统。

### 3.4 详情面板层级

首屏保留：标题、完成状态、日期、优先级、清单。  
次级设置（提醒、重复、标签、子任务、备注）放入分组或折叠区域。  
窄屏使用抽屉展示详情。  
保存中、保存成功、保存失败状态必须可理解，并沿用现有保存流程和字段语义。

## 4. 非目标

- 不修改 Rust command、数据库 schema、同步协议、导入导出格式或 AI 动作解析。
- 不新增营销页式布局、装饰性背景或大型动画。
- 不在本阶段重做全局撤销系统、多选业务逻辑或任务数据模型。

## 5. 交互与视觉原则

- 紧凑但不拥挤：保证扫描效率，避免过大留白。
- 状态不只依赖颜色：完成、逾期、选中需有文字、图标或线条辅助。
- hover、选中、拖拽状态不造成明显布局跳动。
- 主要按钮和任务操作在窄屏下仍可点击。
- 延续现有主题、圆角、间距和控件尺寸体系，不做无关的视觉大改版。

## 6. 测试与验收

### 6.1 自动化测试

优先补充或更新：

- `src/components/__tests__/DayView.test.tsx`
- `src/components/calendar/__tests__/WeekView.test.tsx`
- CalendarView、任务列表、详情面板直接相关测试
- 默认视图非法偏好回退
- 自动定位只执行一次且不干扰手动滚动
- 长标题、多标签、窄屏下的结构渲染
- 详情抽屉/响应式状态

### 6.2 手工验收

1. 宽屏保持三栏工作流。
2. 窄屏无明显遮挡、溢出或横向布局崩坏。
3. 任务行 hover、选中、完成不会跳动。
4. 日/周视图首次进入能看到当前时间附近。
5. 手动滚动后不会被自动定位抢回。
6. 日历默认视图偏好行为保持不变。
7. 长标题、多个标签、逾期和全天任务均可读。

### 6.3 工程验证

- 运行相关前端测试。
- 运行 TypeScript 检查和前端构建。
- 报告修改文件、测试结果、无法自动验证的视觉风险和后续建议。

## 7. 实施顺序

### P0：本阶段必须完成

1. 自适应三栏和详情抽屉。
2. 任务列表固定密度与防布局跳动。
3. 日历当前时间定位、手动滚动保护和视图工具栏一致性。
4. 加载、空状态、错误、保存状态和撤销反馈统一。

### P1：紧随其后

1. 键盘快捷键和命令面板。
2. 多选批量操作。
3. 快速添加语法。
4. 搜索与组合筛选。

### P2：后续品质提升

1. 深色模式和主题细节校验。
2. 键盘无障碍、焦点管理和 ARIA 标签。
3. 更完整的窄屏适配和微交互。

## 8. 预期收益

- 宽屏一次可查看约 18～25 条任务，减少滚动次数。
- 窄屏布局不再强制压缩详情和任务列表，降低误点。
- 日历首次打开即可定位当前时间，减少手动寻找。
- 改期和重复任务操作有明确语义，降低误修改风险。
- 统一状态反馈后，用户能在一次交互内知道“正在保存、已保存还是失败”。
